# whistleWatch

To use:

1. Run whistleWatch.py to latest match data if needed
2. Run generateRefStats.py to grab stats by ref and store the results
3. Run combineData.py which generates allMatches.json (needed because vibe code no work)
